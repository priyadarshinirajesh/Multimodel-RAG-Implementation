1. conda env create -f environment.yml
2. conda activate mmrag-clinical
3. pip install -r requirements.txt
4. python -m ingestion.ingest_to_qdrant
5.python -m tests.test_qdrant_basic
6. python -m tests.test_patient_retrieval
7. uvicorn api:app --host 0.0.0.0 --port 8000 --reload

also create a .env file in the main folder to store GROQ_API_KEY (go to groqcloud and generate your api key)
after creating the .env file 
add the following line to the .env file

GROQ_API_KEY=YOUR_GROQ_API_KEY

mkdir -p pathology_detection
mkdir -p pathology_detection/models
mkdir -p pathology_detection/weights
mkdir -p pathology_detection/training
mkdir -p pathology_detection/evaluation
mkdir -p pathology_detection/visualizations
mkdir -p data/processed  # For processed labels

python pathology_detection/training/prepare_dataset.py

python pathology_detection/models/densenet_classifier.py

python pathology_detection/training/train.py

python pathology_detection/evaluation/evaluate.py

python tests/test_pathology_integration.py


